# WebRTC-Javascript
WebRTC implementation from javascript from scrath.
Run `npm i` and then `npm start` in order to boot the app.
Open two tabs and go to port `http://localhost:3000/` in order to establish the WebRTC connection.
